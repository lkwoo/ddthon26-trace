"""C1 MCP 서버 어댑터 (UOW-05).

5개 MCP 도구를 코어 함수(engine)에 1:1로 배선하고, 지식 리소스·프롬프트를 노출한다.
얇은 어댑터 — 비즈니스 로직은 코어에 있으며, 여기서는 입력·루트 주입·직렬화·오류 격리만 담당한다.

주의: `mcp` SDK는 server 모듈에서 지연 임포트한다(코어·대다수 테스트는 mcp 불필요, NFR-05-TST-3).
"""
