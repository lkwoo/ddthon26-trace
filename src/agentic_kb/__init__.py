"""Agentic Knowledge Base (MCP-based).

A dual-interface knowledge engine: deterministic structured extraction of code
and Markdown into a file-based (Markdown + JSON) knowledge base, consumed by LLM
agents via MCP and by humans via a lightweight web viewer.
"""

__version__ = "0.1.0"
